import React from 'react'

function NoteSection() {
    return (
        <section className="note">
          <h3>NOTE FROM THE EDITOR</h3>
          <p>We are building Bitcoin backed Internet using trust minimized verification & proof aggregation for all layers & apps.</p>
        </section>
      );
}

export default NoteSection